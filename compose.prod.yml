services:
  nginx:
    ports:
      - 80:80
  node-app:
    build:
      context: .
      args:
        NODE_ENV: production
    ports:
      - 3000:3000
    environment:
      - NODE_ENV=PRODUCTION
    command: node index.js
